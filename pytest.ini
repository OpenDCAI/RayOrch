[pytest]

norecursedirs =
    test/legacy
    test/manual
    build
    dist
    dataflow

markers = 
    cpu: mark test to run on CPU
    gpu: mark test to run on GPU
    api: mark test to run API related tests
    slow: long-running performance or stress test, skipped unless --runslow is used
